---
date: 2026-06-29
commit: pending
feature: chat-file-actions-open-artifacts
impact: Chat file-card action buttons now open the file in the Artifacts drawer instead of downloading immediately, so downloads happen from the artifact detail toolbar.
---

# Chat file actions open artifacts

Generated file cards in chat no longer download directly when their action button is clicked. The action now opens the file in the Artifacts drawer, where the user can preview the output and use the artifact toolbar download button if they want to save it locally.
